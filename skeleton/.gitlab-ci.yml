deploy:
  stage: deploy
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  script:
  - aws configure set aws_access_key_id ${AWS_ACCESS_KEY} --profile default
  - aws configure set aws_secret_access_key ${AWS_SECRET_KEY} --profile default
  - aws configure set region eu-central-1 --profile default
  - aws s3 cp artifacts/airbyte_snowflake_dag.py s3://${{ values.destinationBucket }}/${{ values.destinationDag }}
  - aws s3 cp artifacts/snowflake.sql s3://${{ values.destinationBucket }}/${{ values.destinationSql }}
  environment: production
